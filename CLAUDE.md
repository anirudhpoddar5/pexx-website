# PEXX

**Read `AGENTS.md` in this directory. It is the entry point for this repo**, and
it points at the shared standards in `.agents/standards/`. This file is a
pointer, not a second copy — two copies of the same rules drift, and the
divergence is invisible until an agent acts on the stale half.

Start with `.agents/standards/SKILL-ROUTING.md`: it maps the task you are about
to do to the skill you should invoke first.
